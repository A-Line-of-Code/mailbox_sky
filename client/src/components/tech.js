import React from "react";

export const Tech = (props) => {
   
    return(
        <div>
        name = {props.name} <br />
        img = {props.img} <br />
        msg = {props.msg} <br />
        </div>
    )
    
}
